# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Bash@3
  inputs:
    targetType: 'inline'
    script: |
      # Write your commands here
#      pip install -U wheel
#      pip install tornado
#      pip install mkdocs
      pip list
  displayName: 'installing required packages'
- task: Bash@3
  inputs:
    targetType: 'inline'
    script: |    
#      mkdocs serve
  displayName: 'building the mkdocs project'